# 现场采集清单

网站代码已为校园地点、建筑入口和楼内二维码定位点留出数据字段。**没有现场数据时，室外步行路线和真正的自动室内蓝点不会启用。** 请把采集结果发给开发者录入，不需要自行改代码。

## 1. 校园基本信息

记录学校全名、校区名称、详细地址，以及可以公开使用的校区平面图。标注教学楼 C、图书馆、食堂、宿舍和体育馆的正式名称；不存在的类别不必补齐。

## 2. 每个建筑的步行入口

请实际站在**人能进入建筑的门口**，为每个入口记录：

| 字段 | 记录方法 |
| --- | --- |
| 建筑和入口名称 | 例如“C 教学楼南门”；注明学生是否可通行、开放时间。 |
| 高德坐标 | 在高德地图上长按入口点，记录经度、纬度，明确写“GCJ-02 / 高德坐标”。不要把浏览器 GPS 的 WGS84 数值直接当作高德坐标。 |
| 现场证据 | 拍门口照片；记采集日期、手机/电脑、坐标精度和实际通行情况。 |
| 室外连通性 | 从校园道路走到门口，记录围栏、台阶、封闭通道和无障碍入口。 |

优先采集 C 教学楼实际入口，之后逐个增加图书馆、食堂、宿舍、体育馆。只有经过核对的入口才能启用步行导航终点。校园内独立小路若高德没有收录，还需记录步道节点和连线，后续添加校内步行图；不会把高德返回的道路路线冒充校园内部全部可通行路线。

## 3. C 楼四楼室内锚点与路线

当前示例 `C-F4-WEST-STAIR` 来自图片，图上坐标 `(15.7%, 61%)`、通道节点 `l16_61`，**尚未现场核对**。请到 C 楼四楼西侧楼梯平台，确认图片上的点确实对应楼梯出口，再记录：

| 字段 | 记录方法 |
| --- | --- |
| 锚点 ID、建筑和楼层 | `C-F4-WEST-STAIR`、`C`、`4`；在楼梯或电梯实际出层处拍门牌。 |
| QR 张贴位置 | 记录二维码贴在平台哪一侧、朝向和照片；同一个 ID 只能贴在对应点。 |
| 图上点和通道 | 在当前四楼图片上标出脚下位置和相连走廊，核对 `(x,y)` 百分比与 `navigationNode`。 |
| 通行验证 | 从该点实际走到几间教室，记录门口、转弯、封闭门和楼梯是否与蓝线一致。 |

确认后才能长期张贴该锚点二维码：`https://kailell.github.io/campus-floor-guide/?anchor=C-F4-WEST-STAIR`。扫码会读取锚点；若同一浏览器最近两小时内已选教室，或网址带 `&target=C120406`，会继续生成室内路线。其它楼层需提供该层底图、楼层号、可走通道、教室入口和楼梯连接，不能从四楼图推断。

## 4. 权限与设备实测

请各用一台手机和一台电脑打开公开 HTTPS 网址，记录浏览器名称、网站定位权限、系统定位服务是否开启、页面给出的诊断代码、估计精度和更新时间。手机在室外测试一次、C 楼内测试一次；若在微信内打开失败，也在系统浏览器测试并截图。系统定位服务状态无法由网页直接读取，只能通过设备设置检查。
