// Fill these only after obtaining a Web(JS API) key and an HTTPS security-code
// proxy. Never commit securityJsCode to this public GitHub Pages repository.
window.CampusMapConfig = Object.freeze({
  amapKey: '',
  amapServiceHost: ''
});
